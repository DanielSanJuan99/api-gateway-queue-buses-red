# ============================================
# Variables de Entorno - API Gateway Buses RED
# ============================================
# Copia este archivo como .env y ajusta los valores según tu entorno.
# Uso: docker run --env-file .env ...
#      o exportar manualmente: export $(cat .env | xargs)

# ============================================
# SEGURIDAD (Basic Auth)
# ============================================
# Credenciales para autenticación en el gateway
GATEWAY_USERNAME=admin
GATEWAY_PASSWORD=admin123

# ============================================
# NOTAS
# ============================================
# 1. NO commitees el archivo .env (solo .env.example)
# 2. Estas credenciales protegen todos los endpoints del gateway
# 3. Los endpoints /actuator/** están permitidos sin autenticación
# 4. En docker-compose.yml estas variables ya tienen valores por defecto
